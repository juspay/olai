/** The same order is used by the sidebar and identity-free notification clicks.
 *  It lives on the static door (`../../attention.ts`) so a tab's dot reads the
 *  same rule. */
export { needing } from "../../attention.ts"

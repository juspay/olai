/**
 * WHAT `@olai/tests` READS OF THIS PLUGIN'S OWN CLIENT — three answers a
 * scenario cannot re-derive, and no components.
 *
 * ## Why a door at all
 *
 * `@olai/web`'s `./testlib` is the curated list the e2e suite reads core's own
 * client through, and its header argues the shape: a number typed twice
 * eventually disagrees, so a step that waits out a deadline waits out the SAME
 * constant the client uses, and a selector that names a class names the one the
 * component was drawn with. Three of the entries on that list were chat's, and
 * they came here with the panel.
 *
 * ## NAMES AND NUMBERS ONLY
 *
 * Nothing on this door draws. That is the same fence `./testids.ts` keeps one
 * file over and for the same reason: a suite that runs in Node must be able to
 * spell this plugin's DOM without a SolidJS component — and behind one of these
 * modules is the whole transcript. What crosses is a selector fragment, a
 * duration, and one pure reading of an armed composer.
 *
 * `@olai/bundle`'s `fence.test.ts` records the suite's import of this door as an
 * equality, exactly as it records the two files that reach kolu's fake padi: a
 * general package importing a plugin is refused, and the two that are allowed
 * are written down rather than excused by a pattern.
 */

/** The attribute a rendered node reference wears — what a step locates a
 *  pressable backtick by, in the transcript and in a backlink alike. */
export { NODE_REF } from "./browser/chat/refs.ts"

/** How close to the foot of the transcript still counts as FOLLOWING — the
 *  number the panel's own autoscroll decides with, so a step that scrolls and
 *  then asserts is asserting against the client's answer rather than a second
 *  guess at it. */
export { NEAR } from "./browser/chat/near.ts"

/** WHICH TRIGGER THE COMPOSER HAS ARMED for this draft — the client's own
 *  question, asked rather than re-decided, because no attribute carries it. */
export { completingIn } from "./browser/chat/completion.ts"

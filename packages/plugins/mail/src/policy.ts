/** Consent follows the declared kind, including re-keyed columns, while disabled. */
export const writeReservations = [{
  key: "mail-inbox", kind: "mail-inbox",
  says: "inbox wakes are a person's choice on the agent node, never an agent's own opt-in",
}]

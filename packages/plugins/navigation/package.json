{
  "name": "olai-plugin-navigation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./contract": "./src/index.ts",
    "./browser": "./src/browser.tsx",
    "./*": "./src/*",
    "./routing": "./src/routing.tsx",
    "./routes": "./src/routes.ts",
    "./workspace": "./src/workspace.ts",
    "./landing": "./src/landing.ts",
    "./pane": "./src/pane/context.tsx",
    "./opens": "./src/opens.tsx",
    "./palette-model": "./src/palette/items.ts",
    "./palette-asking": "./src/palette/asking.ts",
    "./palette-open": "./src/palette/open.ts",
    "./slots": "./src/slots.ts",
    "./testids": "./src/testids.ts",
    "./address/address.ts": "./src/address/address.ts",
    "./address/Face.tsx": "./src/address/Face.tsx",
    "./routes.testlib.ts": "./src/routes.testlib.ts"
  },
  "//routes.testlib": "The route VOCABULARY, and a door rather than a file another package reaches around for. It is the table both readings of one grammar are asked of — this row's bijection (src/routes.test.ts) and outlines' delegation (its browser/address.test.ts) — and it was @olai/web's `client/routes.testlib.ts` until this row owned the grammar, which made a general package spell a plugin to build it. A plugin may reach another plugin ONLY through a static contract door, benches included, so the second reader is why this is listed in `olai.contracts` too: undeclared, that import is red in @olai/bundle's fence.test.ts under \"plugins consume other plugins only through static contract doors\". It carries data and no runtime, which is what lets it be a door at all.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@olai/plugin-api": "workspace:*",
    "effect": "4.0.0-rc.112",
    "solid-js": "^1.9.0",
    "olai-plugin-ui-renderer": "workspace:*",
    "@olai/web": "workspace:*",
    "@olai/format": "workspace:*",
    "@olai/surface": "workspace:*",
    "olai-plugin-outlines": "workspace:*",
    "@solid-primitives/keyed": "^1.5.2",
    "olai-plugin-layout": "workspace:*",
    "@olai/edit-history": "workspace:*",
    "olai-plugin-vault": "workspace:*",
    "@olai/ui-primitives": "workspace:*",
    "olai-plugin-search": "workspace:*"
  },
  "olai": {
    "contracts": [
      "./routing",
      "./routes",
      "./workspace",
      "./landing",
      "./pane",
      "./opens",
      "./palette-model",
      "./palette-asking",
      "./palette-open",
      "./slots",
      "./testids",
      "./address/address.ts",
      "./address/Face.tsx",
      "./routes.testlib.ts"
    ]
  }
}

/**
 * THE PLUGIN'S WORD, and the whole of this door.
 *
 * A tenant's root entry is its WIRE IDENTITY — the sibling surface it composes
 * and the faces it exposes on it. An engine composes none: what an engine
 * contributes is a row of the chat panel's picker and the readings behind it,
 * and every one of those already travels on the chat cell, which is core's. A
 * second surface saying the same thing under `surface/claude/` would be one fact
 * on the wire twice.
 *
 * So what is left is the name, and it is here rather than in either half because
 * BOTH halves need it and neither may be the other's source: `./server.ts` is
 * mounted by the row and `./browser.tsx` is the chunk the tab fetches, and they
 * share no graph at all. It IS the row's `id` — the word the fiber is bound
 * under, the word `--plugins` takes, the key the tab's slot table stamps a mark
 * with, and the address of this plugin's docs page — and
 * `@olai/bundle`'s `composition.test.ts` holds the two equal by loading the
 * module its row names and reading this.
 */

export const name = "claude"

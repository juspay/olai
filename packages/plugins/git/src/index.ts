/**
 * THE GIT PLUGIN — the ledger, as a row.
 *
 * `@olai/git` was a general package and `@olai/ops` held what is waiting and
 * the quiet-window loop. They are this row now: a serve that does not name it
 * has no pill, no `surface/git/` on the wire, and `ops.commit` refuses in
 * words. Writes still land; nobody records them.
 */

export { faces, name, surface } from "./wire.ts"

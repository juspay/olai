/**
 * A short name for a pane — the rail, the tab, the header. Navigation's
 * `Routing.label`, which is where the naming lives now that a second row (the
 * tabs strip) names pages too; this is the pane's way to it.
 */
export { labelOf } from "../routing.ts"

{
  "name": "@olai/surface",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./holding": "./src/holding.ts"
  },
  "//exports": "`./holding` is a subpath rather than a re-export from the index for one reason: it is the only module here that reaches for `@kolu/surface/server`, and the index is what the browser bundles. A serving-side seam behind its own door costs a client nothing — the arrangement @olai/acp's `./wire` already uses.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "//dependencies": "@olai/format for the domain's wire vocabulary, and @olai/acp's `./wire` subpath for the protocol's — the surface is a declaration, and every olai type on the wire is owned by the package that speaks it and re-exported here (RepoState through format, the ask shapes through acp). @kolu/surface is absent on purpose: it is hydrated from the Nix store rather than installed from bun.lock, so it is declared once for the whole tree in the root package.json (see bunfig.toml).",
  "dependencies": {
    "@olai/acp": "workspace:*",
    "@olai/format": "workspace:*",
    "effect": "4.0.0-beta.106"
  }
}

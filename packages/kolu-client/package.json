{
  "name": "@olai/kolu-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "//": "HOW OLAI REACHES KOLU — see src/index.ts, which argues the whole boundary. What this manifest is FOR is the wall: a package cannot depend back on @olai/server, so the direction is physics rather than discipline.",
  "//dependencies": "@olai/surface is the vocabulary everything here produces, and it is the ONLY olai package this one names — which is the boundary as a fact rather than a comment. @olai/format is deliberately absent: the walk over the vault belongs to whoever holds the vault (@olai/server's claimants.ts), and what crosses into this package is four strings per claim (src/fleet.ts's Claimant). @kolu/padi-client, @kolu/surface and @kolu/terminal-vocab are absent for @olai/surface's reason (bunfig.toml): they are hydrated as raw TypeScript from the Nix store rather than installed from bun.lock, so they are declared once for the whole tree in the root package.json, and scripts/check-kolu-deps.sh keeps that list honest. This is the ONLY olai package that imports them.",
  "dependencies": {
    "@olai/surface": "workspace:*",
    "effect": "4.0.0-rc.110"
  }
}

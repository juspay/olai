/**
 * WHAT EACH ENGINE SAYS ABOUT BEING INSTALLED — the whole of what the no-agent
 * face lists.
 *
 * An ACP engine is a PLUGIN, so how a person GETS one is a drawing about a
 * plugin — which is exactly what `@olai/bundle`'s `fence.test.ts` holds no
 * general package may hold a table of. `../plugins/marks.ts` is the sibling one
 * slot over, and its header argues the shape: a slot rather than a table,
 * because a table here would be a core file edited every time an engine core has
 * never heard of ships.
 *
 * ## THE LIST RATHER THAN A LOOKUP
 *
 * Because this face has no other source for WHICH engines to name: the roster it
 * would ask is empty by definition there, and the slot table is what a serve
 * composed. An engine that hangs no face is simply not listed, which is the
 * honest answer for one with nothing to say about being got.
 *
 * That is also why there is no second function here for the PICKER'S row. Its
 * words are the engine's `name`, which the server sends per installed agent on
 * the chat cell — a string core draws rather than composes, and one authored
 * source rather than two. A face for it existed and drew, at all three engines,
 * exactly the markup core falls back to.
 */

import type { JSX } from "solid-js"

import { hung } from "./runtime.ts"

/** Every engine's own sentence about being got, in the build's order. */
export const installs = (): ReadonlyArray<{
  readonly plugin: string
  readonly face: () => JSX.Element
}> => [...hung("chat.agent.install")]

{
  "name": "@olai/child",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "//dependencies": "None, and that is the shape: this is the subprocess socket, a LEAF beside log and identity (docs/architecture.md). node:child_process is a builtin. Effect is deliberately absent — git and chat wrap in their own language; a leaf that spoke Effect would put a package between a caller and the ability to start a process. A workspace sibling here would be the same inversion @olai/log exists to have stopped.",
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}

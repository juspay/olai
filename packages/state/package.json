{
  "name": "@olai/state",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "//dependencies": "`effect` and nothing else, deliberately: this is a LEAF beside `format`, `git` and `log` (docs/architecture.md), and it is the whole point of the extraction — a package that knows where a machine keeps files and knows nothing about outlines, git or a wire. A workspace sibling appearing here would be that knowledge coming back.",
  "dependencies": {
    "effect": "4.0.0-rc.112"
  },
  "//devDependencies": "@types/node is for `node:fs`, `node:crypto` and `node:os`, which this package IS.",
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}

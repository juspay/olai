# Finishes

The document `install` attaches with its `doc` field. Its only job in this
corpus is to exist: `doc` is a relative path to a served `.md` file, and the
validator rejects a set whose `doc` points at nothing.

- Doors: matte, not gloss.
- Handles: brushed brass.

# The older one

Written before the `Daily/` layout existed, and never moved. The day lists
**both** rather than picking one: there is no conflict rule to invent over two
files a person wrote, so path order is the whole of the answer.

---
agent: grok
worktree: .worktrees/tp
---

# The typed-properties brief

The document a `doc`-typed property points at, so the fixture has one for
`brief` to name and the validator has something to resolve. Its own frontmatter
carries two declared keys, so the chips a DOCUMENT page draws are read out of
the same answers a row's are.

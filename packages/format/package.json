{
  "name": "@olai/format",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "//dependencies": "The bottom of the layering (docs/architecture.md): the format core imports nothing but Effect, so nothing it validates can reach a disk, a server or a browser. A workspace sibling appearing here would be a layering violation, and `bun install` is what would report it.",
  "dependencies": {
    "effect": "4.0.0-beta.106"
  }
}

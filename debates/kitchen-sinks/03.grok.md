# grok — round 3 (close)

Fable retired all four package lifts. Opencode withdrew "the GitState mirror is necessary" and the strong form of the ndjson charge. The list below is what I would actually do. Nothing else.

## Final disposition

1. **Honesty, any day.** Six `@olai/web` imports, not one and not four. Fix `//exports` / `//dependencies`.
2. **`scale.ts` → `theme/`.** Directory, not `@olai/theme`. All three signed.
3. **`GitState` + `gitOf` onto `format/committing.ts`.** One Schema, two importers, surface mirror dies. All three signed. The survey's ops-vocabulary home stays struck.
4. **Adapter interpretation** — `shouldBypass`, `toolNameIn`, `liveModelIn` / `labelsOf` as pure functions (`interpret.ts` or grown `adapter.ts`). Unit tests the `asks.ts` way. Session lifecycle stays in `agent.ts`. Fake-agent skeleton stays in `tests/agent/`; a `bin/` wrapper if the stdin hazard still matters; no `./testlib`, no package.
5. **Grow the Edit union** (`surface/src/edit.ts` + `server/src/edit.ts`). That *is* the two-face axis. `menu-verbs` / `editor-op-parity` are the work. No `surface/git.ts` sibling project while the union is moving.
6. **Tool prose, opencode's knife.** Agent-facing copy in `tools.ts` and person-facing copy in `said.ts` are two reasons. Do not splice them. Each reads from the planner's own structures (`nudge`, refusals, commit policy) so a policy change cannot leave a description behind. Trigger: the verb list is growing now.
7. **`precompress-dev-tax` in place; delete `precompress.ts` on the kolu pin.** Never a leaf.
8. **Zero new packages.** `@olai/markdown`, `@olai/theme`, `@olai/committing`, precompress-as-repo, chat testlib: hold until a dated second consumer exists.

**Not a program:** `said.ts` stays one file (fable now sides here). `reading.ts` / `rpc.ts` / `keys.ts` / `paths.ts` / `pending.ts` vocabulary dump: no. `titles.ts` and `tests` selector/budget constants: mechanical, invent nothing — do them if a PR is already in that file. Do not schedule a Wave 0 so the next survey finds shorter files.

## Conceded this round

- **Opencode on the tools.ts knife.** "Bind or generate" as I first drew it could complect two prose axes. The receptacle is the *policy source*, not a shared paragraph. Target kept; mechanism corrected.
- **Opencode on ndjson.** `support/ndjson.ts` is framing, not olai leakage. Cost of a lift, not falsity of "no olai knowledge." Still not a reason to mint a testlib — the interpret cut makes that package unnecessary.
- **Altitude.** Truly mechanical moves (`titleParts` already exported; World module-scope constants) need no Lowy defence. I was too blanket in calling all of Wave 0 a program. The correction is: mechanical = opportunistic; axis-bearing = named axis. `reading.ts`/`rpc.ts` still fail that test.

## On the record

Fable asked whether any pre-emptive file split is ever justified before a PR touches the file. **No**, except the four axis-bearing items above — and those are not file-length splits. They are the leftover of #122, the adapter override that already exists, the HACKING consistency bug already filed, and two honesty bugs already true today.

`@olai/git`'s socket held bit-for-bit while policy above it churned. That is the bar. The survey measured isolation and ranked length. Isolation selected for stability, which is the inverse of the bar. Do not revive a package lift from a self-contained directory. The walls go where the next three PRs will hit.

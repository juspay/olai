# Round 3 — fable (closing)

Both round-2 turns were written before my round-2 concessions landed, and both independently arrived at the same place I did — which is the strongest evidence the debate produced anything real: three lenses, one list.

## Final concessions

- **`said.ts` stays whole.** grok's round-2 argument settles it: `one-git-indicator` did not only change words, it folded a *face* into the control — a new `Face` is an arm of `faceOf` *and* a row of the copy tables, so the proposed split makes the next face a two-file change of exactly the kind the message-table comment exists to prevent. opencode's axis (presentation churn) is real and *already encapsulated* by the file being one file. My doc's "six vocabularies in a trench coat" line dies; the trench coat was the receptacle.
- **The `commitDoors` parenthetical gets rewritten, not defended.** My sentence was true of `pending.ts` and misleading about the tree (`server/commits.ts:56` interpolates it into `--help`). grok is right that a true-but-misleading citation should be struck; the revision will name the consumer.
- **opencode's mechanical-bar correction is the altitude rule I should have written.** *Mechanical = free, do opportunistically when a PR is in the file; axis-bearing = needs a named axis.* Under it, `titles.ts` and `selectors.ts`/`budgets.ts` pass, `reading.ts`/`rpc.ts` and `surface/git.ts` fail, and "Wave 0" as a *scheduled program* dies — grok's "do not pre-split the tree so the next survey finds shorter files" is the epitaph.
- **On `tools.ts`, opencode's knife beats grok's.** Binding agent-prose to person-prose would splice two axes (a log line and a popover phrase — the recorded `said.ts` justification). The fix is both prose faces reading from the *policy source* (the planner's own typed structures), not from each other. grok's trigger stands: do it when `menu-verbs`/`editor-op-parity` next grow the verb list.

## One correction for the record

grok's round-2 count is right and vindicates the doc's original claim: **six** modules cross the web boundary (opencode's four plus `theme/palettes.ts` and `markdown/scale.ts`). And opencode's round-2 read of `ndjson.ts` partially restores my fake-agent premise — the framing import is a deliberately-shared protocol half, not olai leakage — but the conclusion doesn't change: `bin/` wrapper, no testlib, and the unit-testability payoff comes from grok's interpret cut, not from lifting the skeleton.

## Final disposition — the ratified list

Zero new packages. In order of when:

1. **Honesty, any day:** the two manifest comments corrected against the six real imports; `scale.ts` re-homed beside `palettes.ts`.
2. **#122's leftover:** `GitState` (+ `gitOf`) onto `format/src/committing.ts` beside `RepoState`; ops and surface both import the floor; the hand-kept mirror dies. `COMMIT_MODES`/`whyOf`/`commitDoor`/`commitDoors` stay in ops.
3. **The adapter axis, before the next adapter:** one interpretation module in `chat` (grown `adapter.ts` or `interpret.ts` beside it) owning `shouldBypass`, `toolNameIn`, `liveModelIn`/`labelsOf`, the `mcp__` prefix — unit-tested the `asks.ts` way, no subprocess. Session lifecycle stays in `agent.ts`.
4. **The live receptacle:** `editor-op-parity` and `menu-verbs` grow the Edit union; no sibling-file project in `surface` while the union moves.
5. **On the next verb-list growth:** both prose faces (`tools.ts` descriptions, panel copy) read from the planner's typed policy structures.
6. **Precompress:** `precompress-dev-tax` fixed in place; the file dies on the kolu pin bump per `precompress-upstream`.
7. **Everything else** — navigation-on-touch under the mechanical bar. Never a wave.

The retrodict closes it: `@olai/git`'s socket has never been edited since extraction while everything above it churned — the repo tells us its axes, and none of my four proposed packages sat on one. The doc's survey found the rooms; this debate placed the walls.

# Round 2 — fable

Both openings did what the topic asked: checked the code instead of the summary. I re-verified every disputed citation before writing this, and the honest scoreboard is: opencode lands two of its three factual strikes and misfires one; grok's re-derivation is the strongest single artifact of round 1 and I am adopting most of it. What follows is concessions first (with the verification), then the two rebuttals I'm entitled to, then the disagreement between my two opponents that round 3 should settle.

## Concessions, verified

**Precompress: fully conceded — both of you, independently, and the file itself.** I re-read `precompress.test.ts`: it imports `brotliCompressSync`/`gzipSync` from `node:zlib` and never the util — my "the server already reaches across to test it" was false, and with it half my sharing evidence. Worse, grok's roadmap citation is real: `precompress-upstream` ("Upstream: buildSurfaceClient should emit precompressed siblings") is a filed item my survey missed, and it says the file is scheduled for *deletion* on the pin bump. Extracting a file the plan erases is the exact opposite of paying upstream, and opencode's "duplicated volatility encapsulation — the receptacle is `@kolu/surface-app`" is the correct Lowy reading. Revised position: fix `precompress-dev-tax` in place, delete on the upstream pin, no lift, ever.

**The fake-agent testlib: conceded on the verified premise and the inverted dependency.** `fake-acp-agent.ts:74` imports `../support/ndjson.ts` — the "node builtins alone" half of my claim was wrong (it is a same-package import, not a workspace one, but the clean generic/olai split my lift required still doesn't exist as stated). And opencode's direction argument is the one I can't answer: every `./testlib` in this tree is published *for consumers above*; `chat` publishing a fake agent *for itself* is not that shape. Grok's replacement — extract the *decision functions* (`shouldBypass`, `toolNameIn`, `liveModelIn`) so the plan-mode trap is unit-testable without a subprocess, leave the skeleton in `tests/agent/` behind a `bin/` wrapper until a second consumer exists — achieves the only payoff my version named, at a fraction of the cost. Adopted.

**`@olai/markdown`: conceded down to hygiene.** opencode's §6.5 reading (a package that needs `scale.ts` moved out *first* is a topic-bundle, two axes in one directory) and grok's sharper point — the volatile half (`rewrite.ts`, `tags.ts`: `/media` bijection, picture allowlist, `daily-notes` and `md-editing` land exactly there) is the half my lift would have left behind, and the "server could someday share it" consumer is speculative — together kill the package. What survives: re-home `scale.ts` under `theme/`, and nothing else moves until a real second consumer of the pipeline exists.

**`@olai/theme`: conceded to a directory.** My round-1 hedge ("defensible, not mandatory") loses its "defensible" to grok's likelihood check: fifteen rows shipped at once, one `chrome.ts` since, the OS-preference *removed* — row-supply is variable, the system around it is not volatile. Directory plus the `scale.ts` move plus the declared-contract fix for what tests import. No package. Which means: **zero of my four package lifts survive round 1 as packages.** The doc's first paragraph ("the package layering is not the problem") turns out to be more right than its own package-lift section — opencode's closing line, and I concede it.

**Smaller items:** `keys.ts` split struck (grok: the one-file collision invariant *is* the content, and the coming growth is verbs, not a third keyboard). `format/paths.ts` struck (grok: different allowlists, different inputs — the twin comment documents a rhyme, not a missing function). The length-ranked table: conceded as rhetoric — the inventory stays useful as an *index*, but ranking by `(severity × free)` where severity ≈ lines is, as grok put it, a tape measure. The revision will file every surviving cut under its axis.

## Rebuttals — two, both narrow

**opencode's `commitDoors` strike misfires.** My sentence was "`commitDoors` (which nothing in the *file* even calls)" — a claim about `pending.ts`, and it is true: I re-checked, and the only callers are `server/src/commits.ts:56` and its test. Being consumed *only* from another package while sharing no state with anything in its own file is precisely the co-location-only evidence the sentence was offered as. The strike doesn't change my (conceded) larger position on `pending.ts`, but the record should show the citation was right.

**opencode's `GitState` finding kills a version of the claim nobody should build — grok already shipped the version that survives.** opencode is correct that surface can never import ops: the layering forbids it, the Schema-vs-interface split is forced by the browser boundary, and my doc's "one owner after the vocabulary extraction" was wrong *as written*, because I left the owner's altitude unstated and the natural reading (ops) is impossible. Struck. But grok's move — declare `GitState` once in `format/src/committing.ts` as a Schema, beside the `RepoState` it already re-exports from `@olai/git/state`, and let *both* surface and ops import the floor — is layering-legal (surface already depends on format; that is its only workspace edge), matches the existing `RepoState` precedent exactly, and deletes the hand-kept mirror. So the finding stands as a correction of my doc's altitude, not as a defense of the mirror: the duplication is *not* forced by the browser boundary, only forced out of ops. One Schema, two importers, the mirror dies — #122's missing floor piece.

## What my opponents disagree about — round 3 should settle these

1. **`said.ts`.** opencode: an earned cut on a real axis (presentation vocabulary churns — `one-git-indicator`, copy changes — while the state machine holds; the documented duplication is Lowy's "same change, two reasons, two files"). grok: leave it — one control, one derivation, and the trench-coat metaphor is finding vocabularies, not axes. You are both arguing from the same file header. My stake: opencode has the axis right but grok has the altitude right — copy churn is real volatility, and it is *already encapsulated* by `said.ts` being the one file the copy lives in; splitting it into three would spread one axis across three files. I now side with grok, and note the irony that the prosecution out-extracted the re-framer here.
2. **`reading.ts`/`rpc.ts`.** opencode endorses them "on the axis (payload reading, permission policy), not on line count"; grok says payload readers change when the protocol changes, which is why the *rest* of agent.ts changes — no independent axis, navigation only. grok's version also redraws the cut: adapter-*interpretation* (the `mcp__` prefix, `_meta.claudeCode.toolName`, `_claude/sdkMessage`, `shouldBypass`) versus session lifecycle — which is the cut that still makes sense when `OLAI_ACP_AGENT` points at something that isn't Claude Code. I adopt grok's line: `interpret.ts` (or grown `adapter.ts`) is the receptacle; `reading.ts`/`rpc.ts` demote to move-if-you're-in-the-file.
3. **`tools.ts` prose drift.** grok upgraded my "drift risk to keep in mind" into a first-class duplicated-encapsulation finding with a mechanism (bind or generate the descriptions from the decisions they restate) and a trigger (the verb list is growing *now* — `menu-verbs`, `editor-op-parity`). opencode's round 1 didn't rank it. I ask opencode directly: does the prosecution accept that this — not any file split — is the highest-stakes duplication in the tree, per your own HACKING-consistency axis #1?

## Where this leaves the proposal

The emerging consensus, which the revised doc will carry: (1) the two manifest honesty fixes; (2) `scale.ts` into `theme/`, directory not package; (3) `GitState` onto the format floor, mirror deleted; (4) adapter interpretation as a module with `shouldBypass` unit-tested; (5) `tools.ts` descriptions bound to their sources; (6) precompress fixed in place, deleted on the upstream pin; (7) the Edit union as the live receptacle that `menu-verbs`/`editor-op-parity` grow — no sibling-file project in surface meanwhile; (8) everything else — world/hooks, selectors, titles, reading/rpc, said — demoted to navigation-on-touch, done when a PR is already in the file, never as a program. Zero new packages. If round 3 wants to attack that list, the two open seams are `said.ts` and whether *any* pre-emptive file split (grok's "do not pre-split the tree so the next survey finds shorter files") is ever justified before a PR touches the file.

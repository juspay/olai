# Conclusion — the agreed ledger

Three debaters, three altitudes, all arguing FOR volatility-based extraction per the charter (Löwy's electricity receptacle, the analogy `@kolu/surface` came out of): fable (package altitude), opencode (module altitude), grok (upstream/graduation altitude). Three rounds, converged on everything below except one trigger's exact wording, recorded honestly at the end.

## The finding in one sentence

Argued affirmatively, the electricity analogy demands **three kolu-side socket completions, one recognition of a vault olai already built, and module sockets that pay an honesty tax** — and zero new olai packages, the same number the kitchen-sinks prosecution reached, now confirmed from the opposite direction: olai's package layer is already receptacle-shaped; the leftover voltage sits *above* it (kolu's half-sockets) and *below* it (files).

## The ratified ledger (all three signatures unless noted)

1. **Upstream: `buildSurfaceClient` completed as ONE dist socket.** `precompress` + `splitting` land together (a `precompress` option while `splitting` stays hardcoded leaves the oscilloscope in every consumer's hand). The far side already exists (`freshStaticLayer` negotiates `br`/`zstd`/`gzip`); olai's two re-derived post-steps (`precompress.ts`, most of `markdown.ts`) are DELETED on the pin bump — the deletion is the installation. `precompress-dev-tax` is fixed in place meanwhile. What stays in olai: the Solid transform, Tailwind, fonts, public icons — this house's appliances. (The existing `precompress-upstream` and `splitting-upstream` roadmap items should be ratified as one receptacle, not two chores.)
2. **Upstream: the `connectSurface` readout that cannot lie.** `status` may not say `live` while a subscription over the socket has errored; pending does not degrade (a first frame is what every page load looks like); `needsReload` travels with the payment. The cut: kolu owns the five states' TRUTH, olai's `connection/status.ts` keeps the LOOK — "the files on disk reach this page" is this house's sentence. A two-storey vault; the same class of payment as kolu#2133's stale-tab handshake. Anchor: a receptacle that reads 110 or 220 depending on whether you also brought a voltmeter is not a receptacle.
3. **Upstream: frame-limit chunking.** `chunkBase64` + the envelope/budget helpers pay in beside `RPC_MAX_FRAME_BYTES` (an oversized frame closes the socket and takes every subscription with it; the arithmetic was carried from kolu's padi and every uploading surface-app will re-derive it). Split on `attach.ts`'s own seam: `attachmentRejection`, `isPicture`, `MAX_ATTACHMENT_BYTES`, and the tmp-directory rule STAY in olai — the picture gate is the house's gate.
4. **Upstream, as a fix not a vault: `keysBus`'s O(M·N) broadcast.**
5. **Package: `@olai/store` is recognised as the existing vault.** The workspace package IS the extraction; own-repo is incorporation and waits on the trigger below. Its socket's five early edits were the plug being *fitted*, safely behind the workspace boundary — healthy receptacle history, not shrapnel. No `@olai/markdown` (killed on the domain test: `rewrite.ts`/`tags.ts` answer with the served directory's pictures and Workflowy pills — the house's dialect, not the grid's power; the "markdown" word named two receptacles — the render face stays a directory, the chunk face rides entry 1). No `@olai/theme`, no `@olai/committing`, no disk package, no precompress package.
6. **Module sockets, each shipping a socket-claim test** (fable's amendment, ratified): `interpret.ts` (shipped, the type specimen), the Edit union (in flight — grows arms, the compiler catches non-socket sites), `status.ts` as LOOK policy (shrinks when entry 2 lands), `wire.ts` left whole (the one `connectSurface` import), `disk.ts` store-internal (the voltage inside the store's vault — its shrapnel-path claim was corrected: only `store.ts`/`probe.ts` import it; ops and server plug the store), `attach.ts` (shrinks when entry 3 lands), the `markdown/` directory as its own floor, plus the standing kitchen-sinks items (`GitState` → format, `scale.ts` → theme). The claim test: a grep-shaped unit test per claimed boundary ("only `wire.ts` imports `connectSurface`") — within a package nothing machine-checks import direction, and both debates' stale-manifest exhibits prove hand-kept boundary claims rot. The pattern exists (`clock.test.ts`, the WCAG test); this is the module altitude paying its honesty tax.

## The store-incorporation trigger — agreed core, one open conjunct

All three agree: **the trigger's core is the plug's stillness** — `packages/store/src/index.ts` and its re-exported public types going unedited for a defined window (the `@olai/git` precedent: socket stillness is the product). All three agree a worker/browser FileSystem face landing against the *unchanged* socket is strong evidence; grok's accelerator framing (such a face CLOSES the window early) drew no objection. The open conjunct, recorded as three positions for the human to ratify:

- **fable:** stillness (two release windows) AND the second FileSystem face — both required.
- **opencode:** stillness (90 days — releases don't exist here; `git tag -l` is empty) AND a second consumer of `Store.make` (the face, or any outside app).
- **grok:** stillness alone (two releases, or 180 days as the tagless stand-in), counted from this debate's close; the face is a sufficient accelerator, never a required conjunct; a second consumer is neither necessary nor sufficient. **Registered objection:** writing "dated second consumer" into this conclusion would silently re-litigate Löwy — reuse is a consequence, the still plug is the product.

## Corrections established by citation during the debate

- opencode's `disk.ts` shrapnel path was overstated (only `store.ts`/`probe.ts` import it) and `wire.ts` is two commits, not three — both conceded with re-verification.
- fable's markdown package died at the file level (`rewrite.ts:8–25`, `tags.ts:6–8` are the house's dialect; the security boundary is `sanitise.ts`, and it is already one file).
- fable's `changesOf` footnote withdrawn ("in olai's words" — format's own difference vocabulary, correctly housed).
- grok's store-plug evidence held (`index.ts` edited in #58/#59/#63/#81/#112) and opencode's reframe of it (fitting behind the workspace boundary) was adopted by all three.

## The anchor

The debate's through-line, from the human's own precedent: kolu's surface came out of exactly this thinking — the wire's volatility behind `defineSurface`, extracted at one consumer, reuse arriving afterward as the article says it does. The receptacles this debate demands are mostly completions of that same socket family: the dist, the readout, the frame. The house is well-wired; what it needs is for the grid to finish its half.

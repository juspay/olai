# Round 3 — fable (closing)

The package altitude closes with the honest result it converged toward in round 2: **zero new manifests — and this time from the affirmative direction.** The first debate reached that number by prosecution; this one reached it by arguing *for* the receptacle at every altitude and discovering that the extractions the electricity analogy actually demands are (a) three upstream socket-completions in kolu, (b) one recognition of a vault olai already built, and (c) module sockets named and tested. The analogy did not fail at package altitude; it revealed that olai's package layer is already receptacle-shaped, and the leftover voltage sits above (kolu's half-sockets) and below (files).

## Final concessions

- **Markdown, fully and finally.** grok's round-2 read is the kill my two earlier concessions circled: the socket the four consumers plug is not "markdown in, sanitized HTML out" — `rewrite.ts` answers with the served directory's pictures, `tags.ts` with Workflowy pills, the footnote ids with this app's many-notes-per-page reality. The house's dialect, not the grid's power. And opencode's two-faces dissolution explains why the candidate kept feeling half-right: the *render* face is domain and stays a directory; the *chunk* face is kolu's and rides ledger row 1. The word "markdown" named two receptacles; neither is `@olai/markdown`.
- **`changesOf` withdrawn as electricity.** The file's own header refuses my footnote: "in olai's words." Format's difference vocabulary, correctly housed. (Its `GitState` neighbor still lands beside it per kitchen-sinks item 3 — untouched by this debate.)
- **The store's plug evidence.** grok ran my own socket-stability test against `index.ts` and it came back "still fitting" (five edits, slowing). opencode's reframe — the fitting happened safely *behind the workspace boundary*, re-pointed by the compiler — is the strongest pro-package sentence of the debate, and it argues for exactly what all three of us now sign: the workspace package IS the extraction; incorporation waits.

## The trigger, answered

I asked round 3 to word the store's incorporation trigger. Merging grok's answer (the git precedent: the export list goes a window without growing) with my second witness:

> **`@olai/store` graduates to its own repo when BOTH hold: (1) `packages/store/src/index.ts` and the public types it re-exports go two consecutive release windows without an edit, and (2) a second FileSystem face (the worker/browser layer its own dev-dependency note anticipates) lands against that unchanged socket.** Either failing resets the clock. Until then the package is the receptacle, and threatening it with incorporation is noise.

That is a deferral *with a date-shaped condition* — the thing both debates agreed deferrals must carry.

## One amendment I hold for the record

My round-2 friendly amendment to the module altitude stands and no one has answered it: **each claimed module socket ships a claim test** — a grep-shaped unit test pinning "only `wire.ts` imports `connectSurface`", "no `node:zlib` outside the build", "no format import inside `markdown/` beyond `rewrite.ts`/`tags.ts`" — because within a package, nothing machine-checks the import direction, and both debates' stale-manifest exhibits prove hand-kept boundary claims rot. The pattern already exists in this house (`clock.test.ts`, the WCAG test). If the module altitude is the ledger's biggest section, this is what keeps it honest a year from now. I ask the conclusion to carry it as the module entries' standing condition.

## The ledger I sign

grok's round-2 table, with the amendments above: rows 1–4 (the three upstream socket-completions — dist, readout with the LOOK cut, frame-chunking split on `attach.ts`'s own seam — plus the `keysBus` fix); row 5 (the store vault, incorporation on the two-condition trigger); row 6 (the module sockets: `interpret.ts` shipped, the Edit union in flight, `status.ts` as LOOK policy, `disk.ts` store-internal, `attach.ts` shrinking — each with its claim test). Zero new olai packages, `scale.ts` still re-homes, kitchen-sinks items 3–6 untouched.

The electricity analogy, applied affirmatively, ends where the human's own precedent began: kolu's surface came out of this thinking, and the receptacles this debate demands are mostly *completions of that same socket family* — the dist, the readout, the frame. The house is well-wired; what it needs is for the grid to finish its half.

# grok — round 3 (close)

## Concessions

Fable adopted vault-vs-incorporation and killed `@olai/markdown` on the domain test. I take both as signed. Opencode's correction stands: `index.ts` moved five times because the socket was *fitted* (`commit`, collection re-key, `unreadable`), and that fitting happened behind the workspace boundary. Healthy. It is why the vault is already the package; it is not a reason to incorporate it this week. Opencode's split of the markdown *word* — render directory stays, chunk goes upstream — is the dissolution I will ratify. Fable's two conditions on my upstream pair I already held: appliances stay in olai; "pending does not degrade" travels as the default. Signed.

## The trigger (fable's open question)

Not this cycle. The cycle I say **yes** to, exact wording:

> Graduate `@olai/store` to its own repo when `packages/store/src/index.ts` and the types it re-exports (`Store`, `Codec`, `Snapshot`, `Write`, `StaleWrite`, `PlatformFailure`) go **unedited across two consecutive olai releases, counted from the first release after this debate**. If this tree is not tagging, the stand-in is **180 days** from the debate's close with no edit to that file. A worker/browser `FileSystem` face that lands against that *unchanged* socket **closes the window on the day it merges** — it is proof the plug hid the supply side, not a second gate. A second consuming application is neither necessary nor sufficient.

Fable's "two cycles unedited" is the aging I asked for. I will not AND a second FileSystem face as a required conjunct: that is "dated second consumer" in work clothes, and a face nobody writes becomes a deferral with no date. I will not sign opencode's "when a second consumer exists." Reuse is a consequence. The plug being still is the product.

## Final ledger

| # | Altitude | Sign |
|---|---|---|
| 1 | upstream | `buildSurfaceClient`: `precompress` + `splitting` as **one** socket. Fix `precompress-dev-tax` in place. Delete olai post-steps on the pin. Solid / Tailwind / fonts / icons stay. |
| 2 | upstream | `connectSurface` readout: `status` cannot say `live` over an errored sub. Pending does not degrade. App keeps `LOOK`. |
| 3 | upstream | `chunkBase64` + budget helpers next to `RPC_MAX_FRAME_BYTES`. `attachmentRejection` / `isPicture` / `MAX_ATTACHMENT_BYTES` stay. |
| 4 | upstream (fix) | `keysBus` O(M·N). Not a vault. |
| 5 | package | `@olai/store` **is** the vault. Own-repo only on the trigger above. Zero new manifests. No `@olai/markdown`. |
| 6 | module | `interpret.ts`; Edit union; `status.ts` (`LOOK`); `disk.ts` inside store; `wire.ts` left whole; `attach.ts` shrinks with #3; `markdown/` directory; `scale.ts` → `theme/`; `GitState` → format. Socket-claim tests (fable's amendment) welcome; they are honesty, not extraction. |

## Objection, for the record

Do not write "graduate the store on a dated second consumer" into `conclusion.md`. The date is a still plug, not another house. A conclusion that uses opencode's phrase after this wording will have silently re-litigated Löwy.

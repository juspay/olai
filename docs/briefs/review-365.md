# Review brief: PR #365 — background tasks visible in the chat panel (chat-background-tasks-visible)

You are the REVIEWER. PR: https://github.com/juspay/olai/pull/365, branch chat-background-tasks. You are in the author's worktree (`.worktrees/chat-background-tasks`) — read, build, run; do not push.

1. Read `HACKING.md` in FULL; review per it.
2. The scope (roadmap `chat-background-tasks-visible`, two rulings): a background task the agent arms (Monitor, background Bash) is visible — (ruling 1) as its own row with description/elapsed/death-with-exit-code; (ruling 2, folded mid-PR) while OUT it lives on a FIXED HEADER STRIP (the #357 roster precedent) visible regardless of scroll, and its END lands as a fresh row at the transcript's BOTTOM at that moment, the original tool row staying as the arming's historical record.
3. THE SHARP AXES: (a) the pin patch — acp/patches/background-tasks-visible.patch extends #941's approach to Monitor/background Bash against the compiled dist; audit it line-by-line against acp/patches/README.md's from-#941/added-here claims; a patched vendored dist is a supply-chain-shaped diff, so verify the patch does ONLY what the README says. (b) The strip's lifecycle: can a task end while the pane is disconnected/reloading and leave a ghost strip (or a lost death row)? What does a reconnect draw? (c) The bottom-landing death row: does it interleave correctly with a running turn's streaming output? (d) isTaskOut unification — the simplify pass claims the server's count forgot status; check the fix and its test. (e) The 1 Hz clock running for hours under a persistent task — the author flagged it as pre-existing; judge whether it is acceptable or a MUST here.
4. Verify, don't trust: typecheck, unit, the touched e2e features (the_agent.feature + neighbours); the evidence is scripted (SECTION=a-background-task-the-agent-armed bash evidence.sh) — re-run if cheap.
5. Post a PR COMMENT: OBJECT or DO-NOT-OBJECT, MUST/SHOULD/NIT with file:line, MUSTs with a failure scenario.
6. Report in this terminal: verdict, comment URL, one line per MUST.
# Browser coverage audit

This audit tracks user workflows during the Cordis migration. A passing suite is
necessary, but does not establish that an untested workflow works. A row remains
open until its UI actions and relevant failure/persistence paths have been
reviewed, exercised, and backed by assertions that observe their outcome.

Full-suite validation uses `just ci` or `just e2e-fast-remote`. Targeted browser
runs use the worktree's dev server. Real-harness exploration uses Claude/Sonnet
in a disposable vault; deterministic ACP fixtures make regressions repeatable in CI.

| Domain | User actions and edge paths to audit | Existing feature families | Audit status |
| --- | --- | --- | --- |
| Launch and vault selection | Ordinary, empty, nested, malformed and corrected vault; absent optional services; startup failures | `serve_a_directory`, `see_the_outline`, `error_view`, `empty_vault` | Three `empty_vault` scenarios start with no served files: create/write/reload the first outline or document, and discover/edit an external first outline then create again after removal. Three `first_outline_recovery` scenarios cover the only outline having invalid JSON or an unknown parent, live correction followed by editing, and creation of healthy content beside the only broken file. Broader startup and vault-selection review open |
| Navigation | Outline/document links, node zoom, breadcrumbs, back/forward, deep links, missing targets, scroll restoration | `zoom_and_navigate`, `node_permalink_recovery`, `panels`, `documents` | Two `node_permalink_recovery` scenarios verify an open node permalink recovering after removal, then live restoration or relocation to another outline. They assert updated ancestry, destination breadcrumb navigation, Browser Back and successful editing in the current file without reload. Two `plugin_route_history` scenarios additionally verify Back into a disabled journal route, re-enabling it without losing Forward, and restoring/closing a split with its original focus after a plugin rebuild. Both finish by editing successfully. Three `scroll_history_pages` scenarios cover Back and Forward between a tall outline and a taller document, returning to a shortened outline, mouse-wheel takeover of a clamped restoration, retaining the new top position through another history traversal and successful editing (49 passing steps). Remote CI at `15573857` also exposed an intermittent existing zoom/Back scroll mismatch (190px expected, 39px observed); one targeted replay and twelve parallel repetitions pass, so its cause remains unresolved. Broader navigation review open |
| Search and filtering | Palette queries and navigation, filtered edits, tag and property filters, no matches, changed/deleted results, query persistence | `filter_in_place`, `filter_everywhere`, `a_shortlist_says_its_total` | Fixed palette/header results staying stale after external writes by subscribing while the query is open. Four `search_live_results` scenarios cover removed/replacement targets, subsequent navigation/editing, a changing capped total and recovery after an outage. Existing stale-Enter/pointer behavior and count scenarios pass. Two `filter_live_recovery` scenarios cover live replacement of match membership, editing a match out of the result, clearing to reveal the saved edits, and retaining a no-match query through a plugin rebuild before discovering and editing a later match (51 passing steps). Post-plugin-removal edit/search also covered; broader search/filter review open |
| Outline text editing | Create, rename, multiline paste, notes, blur/Enter/Escape, IME, rapid input and refused writes | `keyboard_editing`, `native_timing`, `title_markdown`, `note_density` | Fixed characters discarded while a parked row waits for the prior save; immediate-input regression added. Six additional `outline_drafts_lifecycle` scenarios cover skeleton retention, refused drafts, cancellation, explicit navigation, independent phone tabs and navigation in a neighbouring pane. `outline_file_recovery` adds two external deletion/restoration cases verifying new membership, usable title editing and a retained unfinished row completed in its original position. Reviewing the editor action registry exposed a missing selected-range lifecycle case. Fixed the reconnect dialog treating its focus grab as a user blur and closing title/note editors; same-draft rebuilds now restore the browser selection range. Four `editor_range_lifecycle` scenarios cover split around a selected word, replacing it then undoing, replacement after a connection outage, and retaining a backward selection in a multiline note before replacement and Undo (70 passing steps). Broader editing review open |
| Outline structure | Add, split, merge, indent/outdent, reorder, duplicate, move between files, multi-selection, drag/drop | `split_and_merge`, `duplicate_subtree`, `move_to_picker`, `move_live_search`, `dragdrop_multiselect`, `drag_across_panes` | `move_live_search` verifies replacement of an open picker’s destination, cross-file movement to the new match and Undo restoring the original parent. Fixed plugin rebuilds discarding bulk selections. Three `selection_plugin_lifecycle` scenarios cover retained two-row indent/outdent, preserving the range anchor, and clearing on explicit navigation and Escape. Three `selection_live_changes` scenarios additionally cover removing a picked row, moving it beneath an unpicked parent, and restoring a deleted row without silently selecting it again; subsequent bulk completion checks selected and untouched task statuses and stored marks (60 passing steps). The 41-step `selection_trash_lifecycle` case checks that a rebuild disarms a bulk Trash confirmation but retains the selected rows, then freshly confirms, restores both rows to their original parent and edits restored work. Fixed the Move picker closing and losing its destination query during plugin rebuilds. Three `move_plugin_lifecycle` scenarios cover a retained cross-file move followed by editing, cancellation and a fresh search on another row, and clearing on navigation. Broader structure review open |
| Undo and redo | Text, structure, marks, dates, trash, cross-pane edits, unrelated external changes, refused/stale inverses | `undo`, `undo_plugin_lifecycle`, `undo_pending`, `cancelled_mark`, `a_stale_enter_spends_nothing` | Fixed plugin rebuilds losing committed undo/redo history. Four scenarios cover two successive text inverses, structural undo/redo, another tab’s conflicting retitle, note redo across rebuild and clearing on file navigation. Fixed Undo running ahead of a pending blur-save. Three delayed-response scenarios cover immediate note undo/redo, preserving an older title inverse and navigation clearing before acknowledgement; unit tests also cover refused saves. Broader undo and concurrency review open |
| Marks and trash | Todo/doing/done, parent completion refusal, hide/show, trash/restore/delete, archived links and filters | `done_over_open_work`, `trash`, `trash_concurrency`, `archived_only_in_trash` | Three new two-tab scenarios verify that adding an archived row disarms the old confirmation, a delayed confirmation refuses the larger pile without deleting either row, and restoring the last row elsewhere dismisses the question and permits subsequent editing. Valid re-confirmation empties the reviewed pile. Broader marks/trash review open |
| Properties and relationships | Add/edit/remove properties, schema errors, typed values, references, prerequisites, mirrors and backlinks | `properties`, `typed_properties`, `property_concurrency`, `edge_editing`, `backlinks` | Three new browser scenarios cover concurrent additions of an absent key (including surrounding whitespace), correction after refusal, independent-key additions and undo preserving the other tab’s changes. Fixed long property links overflowing nested phone rows; an additional scenario checks bounds, the unchanged destination, the full editing value and cancellation. Three `backlink_recovery` scenarios cover replacing the reference kind without changing its count, removing and relocating a source with usable navigation/editing, and keeping an opened section through a plugin rebuild while resetting after navigation. Fixed the section unexpectedly collapsing on rebuild. Three `prerequisite_recovery` scenarios verify live renamed and relocated prerequisite links, completion/cancellation unblocking the dependent, completing the dependent itself, and removing/undoing a dependency after a plugin rebuild (75 passing steps). Three `mirror_recovery` scenarios verify source subtree replacement, editing through a mirror into its source file, canonical zoom/Back navigation, edit/undo after a plugin rebuild, and recovery into a relocated source after deletion (73 passing steps). Fixed row link and zoomed prerequisite panels losing their relation and search during plugin rebuilds. Three `edge_plugin_lifecycle` scenarios exercise retained queries, link write/undo, prerequisite write/removal, cancellation with a fresh relation and navigation reset. Existing stale edit/deletion coverage reviewed; remaining relationship and property edges open |
| Dates and journal | Set/clear date and recurrence, complete repeating work, day/agenda navigation and filtering, daily-note creation and edits | `setting_a_date`, `recurring_dates`, `row_forms_lifecycle`, `journal_and_calendar`, `agenda`, `daily_notes` | Fixed unsubmitted date/repeat choices lost on plugin rebuilds and phone pane switches. Eleven new scenarios cover filtered-page retention, apply, cancellation, explicit navigation, parent collapse, independent phone pane drafts, pending submission guards, Escape during a write, late responses after reopening and retained repeat refusals/retry and phone-width constraints for ordinary/deeply nested rows. Three `daily_note_pending` scenarios also cover the pending-write guard, refusal/retry and creation after navigating away. Fixed repeat submissions and late daily-note creation replacing newer navigation. Wider date/journal review remains open; journal first activation and route recovery scenarios added |
| Documents and media | Create/edit/rename documents, headings and links, markdown, HTML isolation, pictures/PDF/CSV, preview failure and file removal | `documents`, `document_editing`, `html_previews`, `pdf_csv_and_pictures`, `document_plugin_lifecycle` | Fixed unsaved document drafts lost on plugin rebuild; eight new scenarios cover retention, conflict baseline, cancellation, independent panes/tabs, phone tab switching navigation in a neighbouring pane and plugin-route reparsing beside a draft. Existing 19 document editing scenarios also pass; `document_file_recovery` adds four external deletion/restoration cases: usable reader/editor recovery, retained drafts with changed or identical restored bytes, and navigating away while the file is missing. Four `media_file_recovery` cases verify decoded image dimensions after replacement/restoration, CSV truncation reset on replacement and CSV/HTML recovery after removal. Fixed a disappearing image briefly fetching its bare URL and producing a redundant 404. Two `document_history_recovery` scenarios cover Back to externally replaced bytes followed by saving and Forward/Back, plus Back to a deleted file followed by restoration, abandoned-draft exclusion and another successful save (39 passing steps). Remaining media and navigation edges open |
| Capture and file creation | Capture from each entry point, empty/refused input, dates, new outline/document, name collision | `quick_capture`, `new_outline`, `new_file_pending`, `document_editing` | Fixed concurrent new-file submissions and newer filenames discarded by late responses. Six scenarios cover both file kinds, pending Enter, corrected names after refusal and dismissal/reopen during a write. Five more scenarios cover late creation after navigation or pane focus changes and opening a background-created document for reading. Fixed late creation taking over the user’s newer view. Four additional cases verify Browser Back and removal of the initiating pane for both file kinds. Three `capture_pending` scenarios cover typing the next thought before acknowledgement, closing/reopening the palette and correcting a pending refused capture. Fixed newer queries overwritten or annotated by old capture responses. Fixed palette drafts resetting on plugin rebuilds. Three `palette_draft_lifecycle` scenarios cover retained pin renaming, explicit cancellation followed by a clean rename, and a retained capture submitted after rebuilding. Two `empty_vault_capture` cases cover first and subsequent captures with no existing files and the phone header’s touch entry into capture, then opening the created Inbox. Five `new_file_lifecycle` scenarios cover retained outline/document filenames and correctable refusals after another tab changes plugins, plus cancelling one kind without clearing the other. Fixed both forms closing and losing their state on rebuild. Four `file_delete_concurrency` scenarios cover references arriving after confirmation, removal/retry, content arriving in an empty outline, and phone confirmation after a rebuild and with a long filename. Fixed deletion choices overflowing the phone viewport. Broader capture review remains open |
| Panes and sidebar | Open/close second pane, independent navigation and focus, pin/unpin, folds, drawer behavior, resize | `second_pane`, `pin_to_sidebar`, `pin_file_recovery`, `folds_are_remembered`, `the_sidebar_sticks` | Two `pin_file_recovery` scenarios verify missing/restored document targets remain navigable and editable through their pin, and a removed/restored pins file replaces the shelf with the new destinations and supports unpinning. Two `pin_rename_concurrency` cases also verify that renaming from an old question cannot overwrite a changed name or destination, retains the draft on refusal, and succeeds after reopening. Fixed pin renames omitting the original-title precondition. `pin_removed_rename` additionally verifies that unpinning in another tab prevents the old question from renaming the archived record; a planner test checks removal, nesting and a replaced active shelf on retry. Broader pane/sidebar review open |
| Chat startup and harness choice | No harness, first activation, harness selection, startup failure, choice during boot, plugin teardown while waiting | `choosing_an_agent`, `chat_plugin_lifecycle` | Lifecycle cases added; broader review open |
| Chat turns and tool results | Send, streaming, follow-up, formatted output, tool details/diffs, refused writes, silent/error turns, subagent output | `the_agent`, `an_answer_leaves_the_rows_standing` | Real Claude/Sonnet read and reply exercised. Two `node_agent_progress` scenarios verify a background node’s plan, plan completion and fresh-session clearing, and independent terminal output/exit status across switches and cancellation. The scripted terminal command now recognizes its variant when node context accompanies the first prompt. Broader turn/tool review open |
| Chat interruption | Queue, steer, cancel, failure while messages wait, background tasks, shutdown during a running turn | `the_agent`, `choosing_an_agent`, `chat_plugin_lifecycle` | Running-plugin teardown and delayed-tab cancellation scenarios added. Stale Cancel now refuses without stopping either node; valid controls still cancel both conversations independently. Remaining interruption edges open |
| Chat drafts and context | Text and @ completion, node chips, refusal recovery, drawer/session changes, stale/deleted context, reload boundaries | `chat_at_completion`, `chat_at_nodes`, `node_context`, `chat_plugin_lifecycle` | Remount draft loss and stale-tab sends fixed. Three new scenarios cover delayed sends with attachment/text recovery, a newer draft typed before refusal and the palette send path. An additional retry scenario proves colliding message IDs cannot resend the other node’s refused message; both original messages remain independently retryable. `chat_live_completion` adds two cases for replacing a removed suggestion while the draft stays intact and clearing/reopening after replacement; both send the selected node to the harness and verify its resolved title. Remaining context edges open |
| Chat attachments | Picker, paste, drop, camera, multiple/unsupported/large files, remove/send/retry, uploads during navigation and reconnect | `the_agent`, `chat_attachments_lifetime` | Drawer loss fixed; ten new scenarios cover remounts, slow reads, new sessions, chat restart, two nodes, same-name isolation, removal and retry. `chat_connection` additionally verifies that an uploaded PDF and unsent text survive an outage and can be sent to the harness after reconnecting. Broader phone, size-limit and connectivity review open |
| Chat questions and permissions | Allow/deny, choices and free text, subagent attribution, plan approval, cancellation, drafts, reload and second-tab answers | `the_agent`, `the_agent_waits_on_you`, `node_agent_questions` | Fixed question IDs shared between agent instances; three new scenarios cover simultaneous node questions, drawer/plugin retention and harness-restart isolation. Late answers across instances also refused in unit coverage; remaining multi-tab and permission edges open |
| Conversation history | List, choose, title, new/clear/compact, restore after reload/restart, unavailable or deleted stored session | `the_agent`, `choosing_an_agent`, `node_agents` | Draft isolation scenario added. Fixed a stale “try again” reopening the other tab’s selected node; one two-tab refused-session scenario verifies stale refusal and independent valid retries. Broader history review open |
| Node agents | Start/assign/open/new session, navigate to node, history, concurrent sessions, subtree fence, deletion/move, stopped harness and reaping | `node_agents`, `node_context`, `node_agent_sessions`, `node_agent_fresh_sessions` | Fixed lost history navigation and history stranded behind unused fresh sessions; Fourteen scenarios cover history, fresh chats, other-node navigation, reload, restart, historical subtree fences, concurrent current turns, cross-file moves, trash/restore, chat restart and rename. Three additional scenarios cover opening an unassigned chat from a current node, from history and while a node awaits an answer; these chats now use the unscoped process without replacing the node conversation. Two `node_agent_connection` scenarios retain a pending question draft and node-history identity through an outage, then exercise answering, current-session return, fresh-chat creation and the full history chain. `node_agent_capacity` opens nine agents against the eight-scope limit, verifies idle eviction, then restores the original session and sends another message. This exposed and fixed upward menus hiding behind the app header; the menu driver now scrolls the menu before checking sticky obstructions. `node_agent_capacity_busy` fills all eight scopes with pending questions, verifies a visible ninth-start refusal and unchanged questions, then answers one and retries successfully while another question remains answerable. Fixed the start verb discarding its refusal result; row-action callbacks can now return refusal text. Two `node_agent_settings` scenarios verify independent reasoning/boolean settings, fresh node sessions restoring defaults, a delayed tab’s refused setting change and a valid retry on the original node. Fixed successful row-menu starts leaving the chat panel closed, previously masked by a helper that opened it. Node-settings cases now wait for the selected node header before interacting. Two more scenarios verify retained settings and conversation identity through a neighboring plugin rebuild and starting directly into the phone chat sheet. The phone case also exposed the last menu entry hiding behind the minimized chat strip; the strip now reports its measured height and downward menus reserve it. Two `node_agent_phone_history` scenarios use long-press, the chat-sheet scrim and the sidebar to revisit a three-session chain after reload, start fresh from history, and preserve/send separate drafts across two node chats. After two intermittent CI failures, connection/history setup now verifies the intended node header and a changed fresh-session UUID before sending; delayed-settings setup verifies the intended node and rendered Reasoning value before freezing updates. Eight parallel repetitions of the affected workflows pass (292 steps), but this does not establish the original failure causes. The 38-step `node_agent_pending_fresh` scenario holds browser updates after requesting Fresh session, verifies the replacement from another tab, attempts a stale Send, then recovers and sends its draft in the original history session and prompts the replacement independently. The stale send is visibly refused and both conversations remain usable. Timed-reaping and remaining edges remain open |
| Plugin lifecycle and dynamic code | Enable/disable/re-enable, waiting/failed states, dependent consumers, first mount, source edits, approval/version changes, rollback | `the_plugin_switch`, `a_plugin_the_vault_defines`, `chat_plugin_lifecycle`, `plugin_navigation_lifecycle` | Eleven lifecycle/workflow scenarios added; remaining edges open |
| Git | Dirty/clean states, selected commits, auto-commit/pause, external commits, refusals, push policy, absent/restored provider | `committing`, `git_state`, `pinned_git_policy`, `pinned_git_pause` | Provider recovery and committing intervening edits covered. Fixed plugin rebuilds losing a typed commit message and silently reselecting excluded files. Three `commit_draft_lifecycle` scenarios verify retained messages, exact selected-file commits followed by committing the remainder, and closing/reopening while reviewing. Two `commit_preparation_pending` scenarios additionally verify a signing refusal with preserved message/exclusions and successful retry, and a delayed successful reply preserving the next message and file selection before committing the remainder. The `commit_on_phone` scenario verifies the banner position, a retained message, selecting only an outline, then committing a long-path document and seeing the banner disappear. Fixed the phone commit banner being appended below the whole page and covered by the chat strip; a dedicated `app.banner` slot now places it below the header. A second phone commit scenario disables and restores Git itself, checking banner removal, one correctly placed banner returning, retained preparation and a successful commit without reload. Broader Git audit open |
| Terminal and CI integrations | Property doors, opening terminals/runs, unavailable services, reconnect, wake scope, delivery while idle/busy/absent | `terminal_door`, `the_doorbell_rings`, `the_conversations_servers`, `the_feed_opens_its_config` | Two `terminal_live_target` scenarios use the real socket-backed padi fixture to change an open terminal property, verify the old viewer closes, open the new target and read its rendered screen, then remove/restore the property and reopen the original terminal (33 passing steps). A third scenario disables Kolu with a live screen open, verifies viewer disposal, restores the plugin and opens/reads/closes two distinct terminal screens. The three-scenario recovery feature passes 58 steps. Broader terminal/CI integration review open |
| Connectivity and freshness | Offline/reconnect, reload mid-operation, server restart, external writes/deletes, invalid intermediate files, stale controls | `the_connection`, `it_stays_live`, `a_frame_leaves_it_standing`, `palette_write_pending`, `palette_command_pending` | Six new scenarios cover delayed palette write/command responses after typing a newer query or reopening. Fixed old responses closing or annotating the newer interaction; successful writes, refusals, notices, unpinning and a chat command covered. `connection_drafts` adds three outage/recovery scenarios: save a retained document draft, refuse an outdated draft after an offline external edit and save again using the new baseline, and complete a parked outline row. `chat_connection` verifies retained text and uploaded bytes remain sendable after recovery. Wider connectivity/freshness review open |
| Multiple tabs | Concurrent edits, shared conversation focus, responses and permissions, plugin changes initiated elsewhere, per-tab drafts and settings | Cross-feature cases | Open |
| Phone and keyboard access | Touch navigation, drawer and chat, scrolling/long-press/drag, viewport changes, focus, shortcuts and dismissal order | `on_a_phone`, `the_missing_chords`, `dismiss_stack`, `input_widgets` | Open |
| Preferences and identity | Theme/font/density, hidden rows, alerts, persistence across reload, identity presence/absence, layout after changes | `preferences`, `preference_editing`, `preference_storage`, `theming`, `fonts`, `who_you_are`, `the_chrome_holds_still` | Three new two-tab scenarios verify live theme/size/density changes while retaining saveable document/note drafts and the position of a parked outline row. One storage-refusal scenario verifies boot, temporary theme/size picks, document saving and reload without persisted preferences. Broader preference/identity review open |

The PR's single bug/fix/test table records confirmed defects and new coverage.
This inventory must retain unresolved rows rather than treating the number of
passing scenarios as evidence that those rows are complete.

For session lifecycle tests, `the harness keeps distinct sessions on disk`
enables the scripted ACP harness's persistent store in the scratch vault.
New sessions receive unique IDs; only prompted sessions can be listed or loaded,
and replay uses their actual recorded messages across harness/server restarts.
This avoids the canned harness's constant session ID and shared replay hiding
node ownership, history, and restoration defects.

# grok — closing (round 3) · upstream-into-kolu altitude

## Withdrawals

- **r1's "errors cell is the receptacle" as a claim about bug 2.** Validity ≠ currency. Settled-`null` publishes nothing (`store.ts:405`); joining `errors` cannot mark an empty cell. Retracted in r2, signed here.
- **r1 small-finding #1 as a cure for this afternoon.** It remains a pin for last-good-with-rows and unreadable-directory (`face.test.ts:213-241`). It is not TRUTH.
- **Reading-2 of the rebase window.** Filed reading, per fable's disk evidence.

Candidates A and B from r1 stay dead. Nothing in r2/r3 put last-good or the doorbell into `@kolu/surface`.

## Verdicts

**This seat: negative.** Zero kolu changes. Fourth sitting, same wall. The grenades landed in vaults that already exist (`@olai/store`, `@olai/format`). That is the article working, which is the only extraction this altitude owed.

**The ledger, signed** — opencode's r2 as fable signed it, with one shape constraint I will not let soften:

1. **MEANING** — format module; doors table projected beside `names`; basis a declared fact; #395's declarations-off-the-wire stands. Not a kolu cell.
2. **VERDICT** — format owns the shaped judgement (`Reach` the seed; `admits` / `implicated` / `summary` / `fatalAtBoot` the face). Store stays generic (`store.ts:8`). Not a framework health verb.
3. **TRUTH / Vintage** — store publishes it. **No successful read without a `Vintage`, and the vintage's verification path does not share `cycle`'s permit.** A wedged loop must yield a *stale vintage a reader can see*, never a fresh-looking set. `resync`'s stamp-folklore retires behind that door. I sign "impossible" for bug 2's class **only of this shape**. A `freshAsOf` the publish fiber writes is still a clock on the lamp, and I still refuse that weaker thing.
4. **Zero new packages, zero kolu.**

**Bug grades, signed as fable wrote them:** 2, 3, 4, door family unrepresentable under the sockets above; bug 1 reduced (set-rule tier writable; which codes brick a boot is the human's line, not a smuggled policy). Guards stay guards: MCP joins the vintage; Banner clamps on top of `summary`.

## Still refuse

The weaker vintage. If the filed conclusion drops "verification independent of the publish fiber," this seat did not sign that conclusion.

Nothing further. The oscilloscope was one afternoon; the missing pins are in two packages this repo already has.

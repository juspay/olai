# Shipping the server

Fixture fiction, like the outline beside it. This file exists so a scenario can
see what a `@doc` node looks like in the two places it is drawn.

## What is in here

- A heading, so the zoomed page has a block to find.
- A list, so it has more than a paragraph.
- Nothing a step matches by accident: every phrase here is its own.
